'use strict';

// app - Write tests around all of your units
// File Read, File Save, Uppercase
// Mock the fs module methods so that your tests don’t use actual files

// I have NEVER EVER mocked fs module methods. I read the documentation on Jest and am a little confused by this. I will ask Adam about it tomorrow.

describe('App test', ()=> {
  it('lives', () => {
    expect(true).toBeTruthy();
  });
});